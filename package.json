{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/petra-canyon-hotel run dev",
    "dev:hotel": "pnpm --filter @workspace/petra-canyon-hotel run dev",
    "dev:api": "python python_hotel_system/app.py",
    "serve:api": "python python_hotel_system/serve.py",
    "build:hotel": "pnpm --filter @workspace/petra-canyon-hotel run build",
    "serve:hotel": "pnpm --filter @workspace/petra-canyon-hotel run serve",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "overrides": {
      "lodash": "4.18.0",
      "path-to-regexp": "8.4.0",
      "picomatch@2": "2.3.2",
      "picomatch@4": "4.0.4"
    }
  }
}
